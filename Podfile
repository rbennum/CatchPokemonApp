# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'

target 'CatchPokemonApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CatchPokemonApp
  pod 'Kingfisher', '~> 7.0'
  pod 'RealmSwift', '~>10'

  target 'CatchPokemonAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CatchPokemonAppUITests' do
    # Pods for testing
  end

end
